export const POSTER_WIDTH_VAR_PROP = '--poster-width';

export const LEFT_START_VAR_PROP = '--left-start';

export const MARKER_SCALE_VAR_PROP = '--local-marker-scale';

export const MARKER_BAR_WIDTH_VAR_PROP = '--local-marker-bar-width';

export const SNAP_STYLE_NAME = 'scroll-snap-type';

export const VIEWABLE_SLIDE_MARKER_ID = 'slide-marker-in-view';

export const MARKER_SLIDE_ID_SELECTOR = (markerId: number) =>
  `markerId-${markerId}`;

export const IS_SCROLL_DEBOUNCE = 300;

export const RAF_SCHEDULER_DURATION = 300;

export const SCROLL_TO_NEXT_SLIDE_TIMEOUT = 5000;
