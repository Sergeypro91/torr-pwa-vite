import { TButtonColor } from './types.ts';

export const COLORS: Record<TButtonColor, string> = {
  primary: 'var(--primary)',
  success: 'var(--success)',
  warning: 'var(--warning)',
  error: 'var(--error)',
};
